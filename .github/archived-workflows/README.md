Archived Workflows

This directory contains workflows that were intentionally disabled by moving them out of `.github/workflows` so they will not run automatically.

Archived files:
- `pr-checks.yml` — PR checks (typecheck, lint, build, tests)

If you need to re-enable a workflow, move the file back into `.github/workflows/` and commit the change.
Triggered test: 2025-12-24T11:51:40.8998549+10:00
